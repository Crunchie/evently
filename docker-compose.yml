services:
  app:
    build: .
    # Run as the host user that owns ./data so the bind-mounted DB is read/writable.
    user: "${PUID:-1001}:${PGID:-1001}"
    # NB: no `ports:` — the app is NOT published to the host. The Cloudflare Tunnel
    # (cloudflared, below) is the only ingress. This is required for the Access-JWT
    # trust model (§8): the app must be unreachable except through the tunnel.
    environment:
      # The compose-level user (below) has no home dir in the image; gunicorn 26's
      # control server otherwise tries ~/.gunicorn at "/" and logs a permission error.
      - HOME=/tmp
      - SECRET_KEY=${SECRET_KEY:?set SECRET_KEY in .env}
      - DEBUG=0
      - DATA_DIR=/data
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:?set ALLOWED_HOSTS in .env}
      - RESEND_API_KEY=${RESEND_API_KEY:-}
      - RESEND_WEBHOOK_SECRET=${RESEND_WEBHOOK_SECRET:-}
      - EMAIL_FROM=${EMAIL_FROM:-}
      - EMAIL_REPLY_TO=${EMAIL_REPLY_TO:-}
      - CF_ACCESS_TEAM_DOMAIN=${CF_ACCESS_TEAM_DOMAIN:-}
      - CF_ACCESS_AUD=${CF_ACCESS_AUD:-}
    volumes:
      - ./data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/healthz').status==200 else 1)"]
      interval: 30s
      timeout: 5s
      retries: 3

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    # In the Cloudflare Zero Trust dashboard, point the tunnel's public hostname
    # (e.g. rsvp.<domain>) at http://app:8000 over this compose network.
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN:?set TUNNEL_TOKEN in .env}
    depends_on:
      - app
    restart: unless-stopped

  litestream:
    image: litestream/litestream:latest
    command: replicate
    # Same host user as the app so backup files in ./data aren't root-owned.
    user: "${PUID:-1001}:${PGID:-1001}"
    environment:
      # R2 credentials for the off-box replica (litestream.yml expands these once
      # the s3 block is uncommented — CLOUDFLARE_SETUP.md §9). Empty until then.
      - LITESTREAM_ENDPOINT=${LITESTREAM_ENDPOINT:-}
      - LITESTREAM_ACCESS_KEY_ID=${LITESTREAM_ACCESS_KEY_ID:-}
      - LITESTREAM_SECRET_ACCESS_KEY=${LITESTREAM_SECRET_ACCESS_KEY:-}
    volumes:
      - ./data:/data
      - ./litestream.yml:/etc/litestream.yml:ro
    restart: unless-stopped
